const styles = theme => ({
    root: {
        display: 'flex',
        width: 600,
        minHeight: 270,
        justifyContent: 'center',
        alignItems: 'center',
    },
    main: {
        display: 'block',
        paddingTop: 10,
    }
});

export default styles